# Pin Robolectric to a fully-supported SDK. The app's compileSdk (36 + minor
# API level) is too new for Robolectric's SDK provider on a clean CI runner,
# which throws UnsupportedOperationException. Tests don't need the bleeding-edge
# platform — run them against a stable API level.
sdk=34
